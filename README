In this version I have eliminated the RFM12 dependency
from the Jeenode Ports libraries.
At the moment the classes:
    * class RemotePort
    * class RemoteNode
    * class RemoteHandler
are excluded but the plugs works fine.